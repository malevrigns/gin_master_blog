-- MySQL 数据库初始化脚本
-- 在运行程序前，请先执行此脚本创建数据库

-- 创建数据库
CREATE DATABASE IF NOT EXISTS blog_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE blog_system;

-- 注意：表结构会在程序首次运行时自动创建，无需手动创建表

